# STORY-00129: 开场动画精修 + 封面居中 + 按钮透明度 (CR-083)

**Epic**: E-027 Cover Polish
**Sprint**: Sprint 30
**Sprint Goal**: 开场动画精修 + 封面居中修复 + 按钮透明度调整
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
Three user-reported polish issues from Sprint 29 review:
1. Intro animation Phase 1 shows an unidentifiable triangle shape — replace with a cute bouncing star
2. Menu constellation appears shifted right compared to Sprint 28
3. Buttons are too opaque and obscure the constellation background

## Acceptance Criteria
- [x] Intro animation: first moving element is a cute bouncing gold 5-pointed star (not a triangle)
- [x] Net shape in intro Phase 2 is a recognizable round bag/pouch (not a triangle)
- [x] Menu constellation is visually centered (bounding-box auto-centering applied)
- [x] Primary button opacity reduced to ~0.82 (semi-transparent on hover restores full opacity)
- [x] Secondary buttons background rgba alpha reduced to 0.30 (was 0.55)
- [x] Shop button background rgba alpha reduced to 0.25 (was 0.50)
- [x] Zero console errors

## Notes
- `_drawNet()` in intro.js: replaced 3-point triangle path with round bag (two arcs + mesh lines + rope handle)
- Added `_drawBouncingStar()`: 5-pointed gold star with wobble rotation, radial glow halo, 3 orbiting sparkle dots
- `BOUNCE_STAR` appears at elapsed=0.5s, fades out at elapsed=4s (when net sweeps in)
- `menu-sky.js` `_buildLayout()`: added post-computation bounding-box centering — computes actual star position min/max, derives dx/dy to align center with (W*0.50, H*0.33)
- CSS: `.btn-primary` opacity 0.82 + hover:1, `.btn-secondary` bg rgba(6,4,18,0.30), `.btn-shop-from-menu` bg rgba(6,4,18,0.25)
