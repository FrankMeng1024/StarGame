# STORY-00356: 微信preview扫码手机黑屏修复验证

**Epic**: Bug Fix
**Sprint**: Sprint 54-mini
**Sprint Goal**: 修复微信开发者工具preview扫码手机黑屏问题
**Points**: 2
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want scanning the WeChat DevTools preview QR code on my phone to show the game, not a black screen.

## Acceptance Criteria
- [ ] `wx.onShow` triggers `boot()` if not already booted (cold start via QR scan)
- [ ] Canvas size re-checked up to 30 frames after onShow before starting navigation
- [ ] Auth/cloud failures are caught and result in offline mode (not crash)
- [ ] `_booted` flag prevents double-boot race condition

## Notes
- Fix already implemented in sprint47-mini (cc19fc2) as STORY-00347
- Root cause was canvas not ready when app starts via QR code cold launch
- Solution: `wx.onShow` + 30-frame `requestAnimationFrame` retry
- Backend auth failure falls back to offline mode via try/catch in boot()
