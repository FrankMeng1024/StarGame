# STORY-00106: 天文摄影图片预加载 — 展厅打开无延迟

**Epic**: E-005 Gallery
**Sprint**: Sprint 20
**Sprint Goal**: 6项用户反馈修复 — 网兜手部对齐/摆角扩大/星星缩小/展厅布局重排/照片预加载/关卡资源预热
**Points**: 2
**Owner**: Frontend Dev
**Status**: Done

## Description
As a user, I want gallery astrophotography images to appear instantly when I open a constellation's detail page, without a loading delay.

## Expected Result
All CONSTELLATION_PHOTOS URLs are preloaded when the levels screen is initialized, so browser cache is warm before the user navigates to the gallery.

## Acceptance Criteria
- [ ] All photo URLs from CONSTELLATION_PHOTOS preloaded via `new Image()` in levels.js
- [ ] Gallery detail page photos load without visible delay on second visit
- [ ] No console errors from preload

## Dependencies
- STORY-00107 (same prewarm IIFE)

## Notes
CR-065: Added `Object.values(CONSTELLATION_PHOTOS).flat().forEach(p => { const img = new Image(); img.src = p.url; })` in `_prewarmAssets` IIFE in levels.js. Photo loading attr changed to `eager`.
