# STORY-00324: 胜利后点击选关不闪烁 — 修复女巫与list并行出现

**Epic**: E-033 跨平台对标
**Sprint**: Sprint 37-mini
**Sprint Goal**: 胜利星星闪烁顺序对标HTML + 修复选关闪烁女巫并行BUG
**Points**: 1
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, when I click "选关" after winning or failing, I should see a clean transition to the level select screen with no flickering or overlapping visuals.

## Expected Result
Clicking "选关" on victory/fail screen: game canvas stops immediately, clean fade to level select, no girl character briefly visible during transition.

## Acceptance Criteria
- [ ] game.js: _btnLevels 点击处理中，先调用 `_cleanup()` 再调用 `fadeNavigate(() => _navigate('levels'))`（与gallery路径保持一致）
- [ ] 检查 _btnNext（下一关）路径：若 _phase !== 'play'，同样先 _cleanup() 再 navigate
- [ ] 截图验证：从result界面点击选关后，截图显示干净的levels界面（无女巫残影）
- [ ] 截图亮度 > 10

## Dependencies
- 无（独立修复）

## Notes
CR-121. 根因：_cleanup() 只在 gallery 路径被调用，levels 路径遗漏。
