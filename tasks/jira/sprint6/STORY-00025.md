# STORY-00025: Verify S4 (star map overlay) fulfills constellation hint requirement

**Epic**: E-003 (Item System)
**Sprint**: Sprint 6
**Sprint Goal**: 场景完整度 (Scene Completeness)
**Points**: 1
**Owner**: QA Lead
**Status**: Done

## Description
As PO, I want to confirm that the existing `star_map` purchasable item (`星图揭示`) already fulfills the S4 Should-Have requirement (constellation star map overlay), so that we can formally close this backlog item.

## Expected Result
QA verifies that: (a) the `star_map` item is purchasable in the shop, (b) activating it during gameplay shows a semi-transparent constellation line overlay, (c) the overlay is visually distinguishable and does not obscure gameplay. If all pass, S4 is marked Done and removed from the backlog.

## Acceptance Criteria
- [ ] `star_map` item appears in shop grid with correct name and price (20金币)
- [ ] After purchasing and entering a level with `star_map` active, `_drawStarMap()` visibly renders constellation lines
- [ ] Overlay is semi-transparent — does not block star/debris visibility
- [ ] Screenshot evidence of star map active in-game
- [ ] Zero console errors

## Dependencies
- None (existing functionality)

## Notes
This is a verification-only Story — no new code. If verification fails, the gap becomes a new Story. Expected outcome: PASS (star_map was implemented in Sprint 4).
