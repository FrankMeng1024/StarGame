# STORY-00352: 图鉴真实天文照片加载修复

**Epic**: Bug Fix
**Sprint**: Sprint 50-mini
**Sprint Goal**: 图鉴内真实图片修复
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want to see real astrophotography images in the gallery detail page.

## Acceptance Criteria
- [ ] Wikimedia Commons HTTPS URLs load via direct `img.src` assignment
- [ ] `project.config.json` has `urlCheck: false` to bypass domain whitelist in DevTools
- [ ] 10s timeout fallback shows error placeholder instead of hanging
- [ ] No `wx.downloadFile` (requires domain whitelist — removed)

## Notes
- Root cause: `wx.downloadFile` requires domain whitelist; `img.src` direct assignment does not
- Fix: replaced `wx.downloadFile` with direct `wx.createImage()` + `img.src = url`
- Added `urlCheck: false` to `project.config.json §setting`
