# STORY-00410: 结算页深空主题 — "星光消逝"标题+星尘粒子+冷蓝调

**Epic**: E-006 界面美化
**Sprint**: Sprint 78-mini
**Sprint Goal**: 结算页深空主题完成 + 游戏性探究
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want the fail screen to feel thematic and immersive rather than plain, so that even losing feels like a cinematic space moment.

## Expected Result
Fail result card uses a deep-space theme: title "星光消逝" (replaces "⏰ 时间到了！"), cold blue color scheme (#4488ff / #223355 gradients), subtle star-dust particle effect.

## Acceptance Criteria
- [ ] Fail card title changed from "⏰ 时间到了！" to "星光消逝" in canvas-path star style (not emoji)
- [ ] Fail card background gradient: cold blue-dark (`rgba(10,20,60,0.96)` → `rgba(20,40,90,0.96)`)
- [ ] Title text color: `#88aaff` (light blue) with subtle blue glow `shadowColor: '#4466cc'`
- [ ] Star-dust: 8-12 small white/blue particles (radius 1-2px) drifting slowly across the fail card area, alpha 0.3-0.6
- [ ] Particles initialized once when fail phase starts (`_failParticles = []`), updated each frame
- [ ] Existing fail card buttons (重试/选关, blue/purple, 44px) unchanged
- [ ] UX AC: The fail screen reads as "space atmosphere" — player feels the loss is meaningful, not just a timeout

## Dependencies
- None

## Notes
Current fail title string is `'⏰ 时间到了！'` in `_drawResultCard()`.
New title can use `ctx.font = 'bold 20px ...'` with canvas path star decorations around it.
Particle state: `let _failParticles = null;` — initialize in fail phase start, drift with `p.y -= p.vy; p.x += p.vx; p.alpha -= 0.002;`, re-spawn when alpha <= 0.
CR-145 requirement (1): fail page deep-space theme.
