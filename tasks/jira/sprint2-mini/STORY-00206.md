# STORY-00206: 游戏屏幕 Canvas 框架 — 背景 + 地景 + 地面角色

**Epic**: E-M01 核心游戏
**Sprint**: Sprint 2-mini
**Sprint Goal**: 核心游戏完整可玩
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want to see the game screen render with a scene-appropriate starry sky background, ground silhouette, and the girl character standing at the bottom center, so that I know I'm in the level and can interact.

## Expected Result
Navigate to 'game' from levels → full-screen Canvas renders sky gradient + star field + ground silhouette (matching level's scene) + simple girl character at bottom center. No gameplay yet — just static scene.

## Acceptance Criteria
- [ ] `navigate('game')` renders the game screen (not menu fallback)
- [ ] Background uses SCENE_PALETTES for the current level's scene index (Math.floor(levelIdx / 5))
- [ ] Ground silhouette drawn via `drawGroundSilhouette()` from scenes.js at H×0.76
- [ ] Ambient star field (60 bg stars, twinkle animation)
- [ ] Girl character placeholder: simple SVG-style Canvas drawing at (W/2, H×0.82), roughly 50×70px
- [ ] Level name shown at top center (constellation nameZh)
- [ ] "← 返回" button at top-left navigates back to levels screen
- [ ] No console errors on render

## UX AC
- [ ] Scene feels visually distinct for at least scenes 0 and 1 (tested by UX)
- [ ] Girl character is visible and identifiable at all viewport sizes

## Dependencies
- Depends on: STORY-00203 (levels), state.currentLevel

## Notes
Girl character: simple stick-figure with hat, ~50px wide. Detailed sprite in Sprint 3 if needed.
game.js navigate('game') currently falls back to menu — must implement showGame() call.
