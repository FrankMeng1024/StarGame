# STORY-00332: 图鉴详情页天文摄影图片修复

**Epic**: E-009 UI完整化
**Sprint**: Sprint 39-mini
**Sprint Goal**: 画廊与商店信息完整化 — 对标HTML版细节补全
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want to see real astrophotography images in the gallery detail, so that I can appreciate the beauty of each constellation.

## Expected Result
The photo carousel in gallery detail loads and displays real constellation astrophotography images. If the current Wikimedia URLs are inaccessible from WeChat mini game, they are replaced with working URLs.

## Acceptance Criteria
- [ ] Investigate why photos show "加载中..." indefinitely — check wx.createImage() onload/onerror callback firing
- [ ] Test 3 Wikimedia URLs from constellations.js by fetching them in wx.request() — log success/failure to console
- [ ] If Wikimedia URLs are blocked by WeChat's domain whitelist: find alternative CDN sources (NASA APOD, ESA, or other open-access) and update constellations.js photos arrays
- [ ] At least 1 photo loads successfully for Orion (level 1) and Ursa Major (level 2)
- [ ] Photo carousel renders loaded images correctly (drawImage fills the PHOTO_H=180px area)
- [ ] On image load failure: "📷 暂无图片" placeholder is shown (existing fallback, verify it works)
- [ ] No unhandled JS errors in the photo loading path

## Notes
CR-129 (mini). Root cause likely: WeChat mini game has a domain whitelist (`request` domain list in app config). Wikimedia CDN domains may not be in the whitelist. Check miniprogram/app.json for `networkTimeout` and domain config. If whitelist is the issue: either add Wikimedia domains or switch to a CDN that's already whitelisted. Alternative: check if wx.downloadFile() works instead of wx.createImage() with external URLs directly. Also verify: does CONSTELLATION_PHOTOS constant exist or do we read from `c.photos` in constellations.js directly — gallery.js line 405 reads `c.photos || (c.photo ? [c.photo] : [])`.
