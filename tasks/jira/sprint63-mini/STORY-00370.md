# STORY-00370: 游戏界面 Sprint A — touch修复 + SVG少女 + 三角网袋

**Epic**: E-007 游戏界面视觉升级
**Sprint**: Sprint 63-mini
**Sprint Goal**: 游戏界面视觉质量提升 Sprint A
**Points**: 5
**Owner**: Frontend Dev
**Status**: Done

## Description

As a player, I want the game screen to show a high-quality SVG girl character and a realistic triangle net bag, so that the visual quality matches the menu and gallery screens.

## Expected Result

1. Game screen shows SVG girl sprite (4 frames: idle/blink/throw/catch) instead of old bezier spacesuit
2. Net shows triangle bag shape (arc mouth + bezier body + mesh lines) instead of old trapezoid
3. Buttons on all screens respond to touch without TypeError crashes
4. All existing gameplay mechanics (physics, collision, scoring) unchanged

## Acceptance Criteria

- [ ] SVG girl renders visibly in game screen (not white/blank) — idle frame visible at rest
- [ ] Triangle net bag visible and shaped like plan B demo (arc opening, tapered body)
- [ ] Touch events work on menu/level-select/game screens — no TypeError in console
- [ ] Navigation: menu → level select → game → fail → back to levels — all 5 steps PASS
- [ ] Net rope origin connects visually to girl's hand (idle: right side low, extend: right side high)
- [ ] Catch flash shows golden sparkles around net head
- [ ] No regression on gallery, menu, levels screens

## Notes

- `globals.js`: Added `_normTouches(e)` in `_wrapDedup` — maps `clientX/clientY` → `x/y` for DOM path touch events. Fixes TypeError at `menu.js:595` when touch came from `canvas.addEventListener` (DevTools simulator path).
- `miniprogram/assets/sprites/girl.svg` created from `docs/demo/girl.svg` (17375 bytes)
- `game.js` `_drawGirl()` at line 1364: SVG sprite with 4-frame animation
- `game.js` `_drawNet()` at line 1393: triangle bag method B from plan
