# STORY-00392: 选关节点内嵌微星座图案+升级版脉冲光晕

**Epic**: E-012 游戏核心完整性
**Sprint**: Sprint 74-mini
**Sprint Goal**: 图鉴4张天文图片 + 高速QA管道 + 结算页视觉完整化 + 选关/图鉴视觉升级
**Points**: 3
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want to see a tiny constellation line drawing inside each level select node, so the nodes feel like living star maps rather than plain circles.

## Expected Result
Each level select node shows its constellation's mini line drawing clipped to the node circle. Unlocked nodes pulse with a soft blue-purple glow. Completed nodes have a gold outer ring decoration (4 tiny star dots at 45° intervals).

## Acceptance Criteria
- [x] Mini constellation drawing inside each node: already implemented via `_drawMiniConstellation()` clipped to circle (levels.js:568-581)
- [x] Node unlocked: `group.glowColor` pulsing halo, 2-layer radial gradient from node center (levels.js:522-535)
- [x] Node completed (best score): gold outer ring `#ffd700` 2px+glow + 4 tiny star dots (r=2.5, at distance r+10, at 45°/135°/225°/315°) — implemented (levels.js:617-638)
- [x] No regression: existing scroll behavior, item overlay, tap hit detection unchanged
- [ ] Screenshot confirms gold ring visible on completed nodes (DevTools QA pending)

**Notes**: CR-138 (Sprint 56 approved). Mini constellation already existed. Added gold ring + 4 star dots for completed nodes at levels.js:617-638.

## Dependencies
- constellations.js data already available in levels.js scope

## Notes
CR-138 (Sprint 56 approved). STORY-00360 was Todo in sprint56-mini — implementing now.
levels.js already has groupColor/glowColor per group and pulse animation. Mini constellation drawing needs:
- Scale factor: nodeR / max(|star.x|, |star.y|) * 0.7 to fit within circle
- ctx.save() → ctx.beginPath() → ctx.arc(cx,cy,r,0,TWO_PI) → ctx.clip() → draw lines/dots → ctx.restore()
