# STORY-00009: 代码质量修复（动态import + 路由规范）

**Epic**: E-001 核心游戏引擎
**Sprint**: Sprint 2
**Sprint Goal**: 完成进度循环 — 玩家能用金币购买道具、在展厅欣赏已解锁的星座
**Points**: 1
**Owner**: Frontend Dev
**Status**: Done

## Description
As a developer, I want the codebase to follow consistent module import patterns and routing conventions, so that future development is predictable and maintainable.

## Expected Result
Two Arch Medium issues from Sprint 1 code review are resolved: (1) dynamic import in complete.js button handler converted to top-level import; (2) any remaining inline onclick attributes replaced with navigate() router calls.

## Acceptance Criteria
- [ ] complete.js: all imports are top-level (no dynamic `import()` inside event handlers or functions)
- [ ] No HTML elements use inline `onclick="..."` attributes — all click events wired via addEventListener or the navigate() router
- [ ] After changes: full navigation flow Menu→Levels→Game→Complete→Levels produces zero console errors
- [ ] After changes: all existing Sprint 1 features continue to function (no regressions)

## Dependencies
- Depends on: STORY-00001, STORY-00004
- Blocks: none

## Notes
Arch Sprint 1 review items:
- "Dynamic import in complete.js btn handler — should use top-level import"
- "Gallery back button uses inline onclick instead of navigate() router"
These are Medium severity — no runtime impact but should be fixed before codebase grows.
