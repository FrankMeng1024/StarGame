# STORY-00232: 选关屏标题文字修复 (BUG-00101)

**Epic**: E-002
**Sprint**: Sprint 9-mini
**Sprint Goal**: 背景音乐 + 静音控制 + BUG修复 — 完成VU前最后清单
**Points**: 1
**Owner**: Frontend Dev
**Status**: Done

## Description
As a player, I want the level select screen to show the correct title text "选择关卡" without garbled characters.

## Expected Result
- Level select screen header shows "选择关卡" correctly
- No text artifacts like "迎天下下"

## Acceptance Criteria
- [ ] `levels.js` title text is exactly '选择关卡' (verify by code read)
- [ ] Text drawn with explicit font setting before `fillText` call (e.g., `ctx.font = 'bold 18px sans-serif'`)
- [ ] No ctx.font set to a value that could cause CJK rendering issues

## Dependencies
None

## Notes
BUG-00101 (Medium) from sprint4-mini QA report.
Root cause hypothesis: ctx.font not reset between draw calls, inheriting wrong font from previous element draw.
Fix: ensure explicit `ctx.font = ...` immediately before the title fillText in levels.js `_loop()`.
